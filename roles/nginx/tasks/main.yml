---
- name: nginxがなければインストール
  ansible.builtin.dnf:
    name: nginx
    state: present
  tags: nginx

- name: nginx.confを配置
  ansible.builtin.template:
    src: nginx.conf.j2
    dest: /etc/nginx/nginx.conf
  notify: restart nginx
  tags: nginx
